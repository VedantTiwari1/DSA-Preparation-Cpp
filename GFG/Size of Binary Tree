class Solution {
  public:
    int getSize(Node* root) {
        // Base case: if the tree is empty
        if (root == NULL) {
            return 0;
        }
        
        // Recursive call: 1 (current node) + left size + right size
        return 1 + getSize(root->left) + getSize(root->right);
    }
};
